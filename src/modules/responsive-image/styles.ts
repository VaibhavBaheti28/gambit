import { css } from "@emotion/react";

export const imageWrapper = css`
    position: sticky;
    overflow: hidden;
`;

export const styledBlurHash = css`
    z-index: -1;
    position: absolute !important;
    top: 0;
    left: 0;
    height: 100%;
`;
